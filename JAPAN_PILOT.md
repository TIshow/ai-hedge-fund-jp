# 日本株・J-Quants検証版

元の `ai-hedge-fund` のパイプラインを、J-Quants V2の日足・決算短信サマリーと東証の100株単位で動かす実験です。実注文は送りません。`momentum` は前日までの20営業日リターンが正なら買い、そうでなければ保有しない単純な比較用シグナルです。`japan-fundamentals.yaml` では、元のBuffettエージェントが決算短信サマリーを読んで判断します（下記「財務データとAIエージェント」）。いずれも投資成果を示すものではありません。

## 上流 2.4.0 への追随（2026-09-26）

上流 `virattt/ai-hedge-fund` の 2.4.0（2026-09-24）を取り込みました。日本株の動作に関係する変更は次のとおりです。

- **約定のタイミング**: 見直し日（週次なら週の最後の営業日）の終値までの情報で判断し、**次の営業日の終値**で約定します。約定の直前に、前日までの情報で判断を見直します。期間の最後の見直し日の判断は、翌営業日が期間内にないので約定されず、結果の`pending`（保留中の提案）に残ります。
- **弱気は空売りではなく「持たない」**: 上流は判断役ごとに買いだけ（Buffett・Munger・Graham・Lynch、このフォークのmomentumも）か、買いと空売り（Druckenmiller・PEAD）かを決め、戦略ごとに`blend: {mode: ...}`で方式を指定します。日本株用の設定ファイルはすべて`long_only`です。
- **設定ファイルの形式**: 先頭に`schema_version: 2`、各戦略に`blend`が必要になりました。日本株用の7つの設定ファイルは移行済みです。
- **このフォークで外したもの**: `RiskLimits.long_only`（上流の買いだけの仕組みと重なるため）と、`_mark_prices`の`require_current_bar`の判定（約定日の終値を必須にする上流の`exact_marks`に置き換わったため）。
- **日本時間の判定**: 上流は「どの日の株価まで確定したか」をニューヨークの日付で決めます。環境変数`HEDGE_FUND_MARKET_TZ`でタイムゾーンを変えられるようにし、J-Quants利用時は`Asia/Tokyo`にします。過去の日付のバックテストには影響しません。

**2026年9月の30銘柄の比較（下記）は、この変更より前の設計（見直し日の終値で約定、弱気は空売り）で実行したものです。** そのときのコードは、タグ`study-2026-09-japan-30`で再現できます。`japan_replay.py`と`japan_media.py`も、そのときの結果ファイル（1週1行の形式）を前提にしています。上流 2.4.0 以降の結果（毎営業日の資産推移を含む形式）には、まだ対応していません。

## 実行

Python 3.11以上を用意し、このソースのディレクトリでインストールします。`uv`があればそれを使います（`asdf`でPythonを管理している場合は`asdf which python`で解釈系を指定できます）。

```bash
uv venv --python "$(asdf which python)" .venv
uv pip install --python .venv/bin/python -e . 'pytest>=7.4,<8'
```

`uv`がない場合:

```bash
python -m venv .venv
.venv/bin/python -m pip install -e . 'pytest>=7.4,<8'
```

どちらの方法も`poetry.lock`を読まないため、依存パッケージは`pyproject.toml`の範囲内の最新版になります。`.venv/`は`.gitignore`の対象です。

APIキーなしでCLI全体の実行を先に確認できます。結果は**架空の株価**です。

```bash
.venv/bin/python japan_pilot_smoke.py > /dev/null
```

`outputs/japan-pilot-smoke.json` に、3回の週次の見直しのうち約定した2回の注文・仮想約定・残高と、約定前の最後の判断（`pending`）が出ます。これは投資成績ではありません。

### 実データ

キーはリポジトリ直下の`.env`に置きます。`aihf`は起動時にカレントディレクトリの`.env`を読み込みます（シェルで`export`した値があればそちらが優先。`~/.hedge-fund/.env`も読みますが、このforkでは使いません）。`.env`は`.gitignore`の対象なので`git add .`でも追加されません。`japan_pilot_smoke.py`は架空のキーを使うので設定は不要です。

エディタで`.env`を作り、次の1行を書いて保存します（値を引用符で囲む必要はありません）。

```
JQUANTS_API_KEY=自分のJ-Quants-V2-APIキー
```

自分以外が読めないように権限を絞り、Gitの対象外になっていることを確認します。

```bash
chmod 600 .env
```

```bash
git check-ignore -v .env
```

```bash
mkdir -p outputs
.venv/bin/aihf japan-pilot.yaml --data-provider jquants \
  --tickers 7203,6758 --backtest \
  --start 2026-03-02 --date 2026-06-26 --out outputs/japan-pilot-result.json
```

APIキーはログ、公開リポジトリ、共有する結果ファイルに入れないでください。上記の日付はコマンド例です。比較対象の1306は2026-03-30に分割・併合がありますが、比較対象は調整後価格で取得するため、この日をまたぐ期間も実行できます。契約プランで取得可能な期間に設定してください。無料プランには12週間の遅延があり、最新日を指定してもその日の株価は取得できません。`--tickers` は現段階では**数字のみ**の4桁または5桁の東証内国株コードを指定します。`.T` 接尾辞も受け付けます。アルファベットを含む証券コードと、銘柄ごとに売買単位が異なるETFなどを売買対象にする機能は未実装です。初回の取得には銘柄・ベンチマークごとに日足のAPIリクエストが発生します。出力はJSONで、注文・仮想約定・残高と各リバランス日の記録が含まれます。

```bash
.venv/bin/python -m pytest hedge_fund/data/test_jquants.py \
  hedge_fund/data/test_jquants_fins.py \
  hedge_fund/signals/test_momentum.py \
  hedge_fund/backtesting/test_japan_pilot.py \
  hedge_fund/backtesting/test_japan_fundamentals.py \
  hedge_fund/features/test_snapshot_anonymized.py
```

## 財務データとAIエージェント

J-Quants無料プランの決算短信サマリー（`/v2/fins/summary`）と上場銘柄一覧（`/v2/equities/master`）から、元のBuffett・Munger・Graham・Lynch・Druckenmillerが読む財務指標と業種を作ります。PEAD（決算サプライズ）はアナリスト予想が必要で、J-Quantsにないため使えません。

- **時点**: 各見直し日より**前日まで**に開示された短信だけを使います（当日開示は、開示時刻が引けの前か後かを日付だけでは区別できないため除外）。上流 2.4.0 以降は翌営業日の終値で約定しますが、この規則は安全側のまま変えていません。訂正された短信は、訂正の開示後に置き換えます。業績予想の修正は使いません。
- **直近12か月（TTM）**: 四半期の数値は期初からの累計なので「今期累計 + 前期通期 − 前年同期累計」で換算します。前年のデータがない期は省きます（年換算はしません）。EPSは純利益 ÷ 期中平均株式数で、株式分割をまたいでも基準がそろいます。
- **評価**: 時価総額・PER・PBRは、各短信の開示日の終値（未調整）とその短信の株式数で計算します。無料プランの株価の範囲（過去2年）より前に開示された短信は、時価総額・PER・PBRが空欄になります。
- **ない項目**: 売上総利益、有利子負債、流動資産・負債がサマリーにないため、粗利率・負債資本比率・流動比率は空欄です。
- **成長率**: 前年同期の累計との比較（前年同期比）です。通期はTTMの成長率と同じです。
- **既知の限界**: 短信は当初発表の値です。事業の分離などで過去を組み替えた会社（例: 2025年度のソニーG）は、TTMや前年同期比が新旧の基準を混ぜます。
- **使える期間**: エージェントは直近12か月の期が4つ以上ないと判断を控えます。無料プランは過去2年分なので、3月決算の会社では2026年2月上旬以降が目安です。

### 実行

LLMのAPIキー（既定のモデルはAnthropicの`claude-opus-5-5`）を`.env`に1行追加します。

```
ANTHROPIC_API_KEY=自分のAnthropic-APIキー
```

`aihf`はAIへの入力と回答のキャッシュを既定で`~/.hedge-fund/`に保存します。リポジトリ内（`.gitignore`対象の`.hedge-fund/`）に置くには、実行時に`HEDGE_FUND_HOME`を指定します。

```bash
HEDGE_FUND_HOME=.hedge-fund .venv/bin/aihf japan-fundamentals.yaml --data-provider jquants \
  --tickers 7203,6758 --backtest \
  --start 2026-02-09 --date 2026-06-26 --out outputs/japan-fundamentals-result.json
```

LLMの呼び出しは、銘柄ごとに新しい短信が出て入力が変わったときだけ発生します（同じ入力はキャッシュを使います）。上の例なら数回程度です。

**重要な注意（先読み）**: LLMは学習データとして検証期間の出来事（株価や決算の結果）を知っている可能性があります。プロンプトには証券コードと業種が入るため、会社を特定できます。過去期間の結果は「財務データの接続と売買の流れが正しく動くか」の確認であり、AIの判断力の検証にはなりません。判断力を測るには、モデルの学習期間より後の期間で検証する必要があります。

### 匿名化モード（先読み対策）

`japan-fundamentals-anon.yaml` は、エージェントに`params: {anonymize: true}`を指定した版です。それ以外の条件は`japan-fundamentals.yaml`と同じです。AIへの入力から次の情報を除きます。

- 証券コード、業種、時価総額（規模）
- 決算期末日と開示日（「latest」「-3m」のように、最新期から何か月前かで表します）

PER・ROE・利益率・成長率と、EPS・BPSなど1株あたりの値は残ります。数字の組み合わせから会社が推測される可能性は残るので、先読みを減らす手段であって、なくす手段ではありません。同じ期間で2つの設定を実行し、判断や理由の違いを比べると、AIが会社名の知識にどれだけ頼っているかの目安になります。

```bash
HEDGE_FUND_HOME=.hedge-fund .venv/bin/aihf japan-fundamentals-anon.yaml --data-provider jquants \
  --tickers 7203,6758 --backtest \
  --start 2026-02-09 --date 2026-06-26 --out outputs/japan-fundamentals-anon-result.json
```

AIへの入力と回答は`.hedge-fund/cache/llm/`に1件ずつJSONで残ります（`user`が実際の入力）。

### 10銘柄での比較

比較の考え方: **会社名あり**は上流と同じ入力で、AIが学習済みの会社知識を判断に混ぜうる状態そのものを一つの実験条件とみなします。**会社名なし**（匿名化）は、会社を特定する情報を伏せて資料の数字だけで判断させる条件です。投資の一般知識（ROEの意味など）は判断に必要なので対象外で、1株あたりの絶対値から推測される余地はわずかに残ります。業種だけ見せる中間の条件は設けません。

`japan-buffett-10.yaml`（会社名あり）と`japan-buffett-10-anon.yaml`（匿名化）は、資金1,000万円・1銘柄の上限15%（150万円）の設定です。株価15,000円までの銘柄なら100株買えます。2つの違いは匿名化の有無だけです。

```bash
HEDGE_FUND_HOME=.hedge-fund .venv/bin/aihf japan-buffett-10.yaml --data-provider jquants \
  --tickers 7203,6758,8306,9432,6501,8058,4502,7974,4063,6098 --backtest \
  --start 2026-02-09 --date 2026-06-26 --out outputs/japan-buffett-10-result.json
```

匿名化版は、設定ファイルと`--out`のファイル名を`-anon`付きに替えて実行します。J-Quantsの回数制限（無料プランは1分5回）はキー単位なので、2つを同時に実行しないでください。10銘柄ではデータの取得だけで8分ほどかかります。

### ルールで選んだ30銘柄での比較（事前の記録）

**この節は結果を見る前（2026-09-24）に書いたものです。** 結果を見てから仮説や読み方を変えた場合は、変えたことと理由を別に書きます。

- **銘柄**: `japan_universe.py`で、2026-02-16時点の上場銘柄一覧から選びます。条件は、プライム市場、TOPIX Core30・Large70・Mid400の各層から10社ずつ、金融業（銀行・金融（除く銀行））を除外、乱数の種20260209、開始日より前に直近12か月の期が4つ以上開示済み。株式分割と最高株価は選定後に報告するだけで、選定には使いません。バックテストは2026-02-16〜2026-06-26、資金2億円・1銘柄の上限5%（全銘柄を100株以上買える）です。
- **エージェント**: BuffettとGrahamを同じ戦略に入れます（`japan-30.yaml`、`japan-30-anon.yaml`）。Grahamの基準にある流動比率・負債資本比率は無料データでは空欄で、これは両方の条件に共通の不利です。
- **AIの知識の測定**: `japan_knowledge_probe.py`で、各社について同じモデルに「知っていること」を聞き、よく知っている度合い（0〜10の自己評価）を記録します。
- **集計**: `japan_compare.py`。同じ資料（`snapshot_hash`）に対する会社名あり／なしの判断を組にします。

仮説:

1. 全体として、会社名ありのほうが「中立」の割合が高い（10銘柄の予備実験では81%対57%）。
2. その差は、Core30のほうがMid400より大きい。
3. 会社ごとの差（中立の割合: 会社名あり − 会社名なし）は、AIの知っている度合いと正の相関がある。

結果を見る前の変更（2026-09-24）:

- 基準日を2026-02-09から2026-02-16に変えました。2026-02-09では、12月末の四半期決算を期限内（期末から45日以内、2月14日ごろ）に出しながら2月10〜13日に発表した会社（リクルート、JTなど14社）が「期が足りない」として外れ、発表日の遅さで選別する偏りになっていたためです。
- 売買する銘柄の株式分割（株数が増える分割）に対応しました。選ばれた銘柄に検証期間中の分割があり、以前の実装では途中で止まるためです。momentumを使う設定では、これまでどおり分割で止まります。
- 資金を3,000万円から2億円にしました。株価の高い銘柄も100株買えるようにするためです。

- AIの知識の測定を先に実行したところ、自己評価の平均はCore30が8.7、Large70が8.1、Mid400が7.2（範囲6〜10）でした。Mid400もAIにとって「知らない会社」ではなく、幅も狭いため、仮説2と3は差や相関が出にくい条件です（バックテストの結果を見る前の気付き）。AIが本当に知らない会社と比べるには、TOPIX Smallやグロース市場まで層を広げる必要があります。

読み方:

- 約30社・約4か月半の探索的な比較です。相関の数値やp値は目安として示し、「効果がある」とは言い切りません。
- 仮説と逆の結果、差がない結果も同じように報告します。
- 収益率は判断の良し悪しの評価に使いません（期間が短く、空売りの費用などが入っていないため）。

### 30銘柄の比較の実行手順

結果は`outputs/`、AIへの入力と回答は`.hedge-fund/`に保存されます（どちらもGitの管理対象外）。J-Quantsの回数制限（無料プランは1分5回）はキー単位なので、J-Quantsを使う手順は同時に実行しないでください。

1. 銘柄を選びます。約15分かかります。選ばれた銘柄の一覧は標準出力に出るので、ファイルに保存しておきます。

```bash
.venv/bin/python japan_universe.py --as-of 2026-02-16 --end 2026-06-26 \
  --per-tier 10 --seed 20260209 --out outputs/japan-universe-2026-02-16.json \
  > outputs/japan-universe-2026-02-16.tickers
```

2. AIがそれぞれの会社について知っていることを聞きます（1社1回、J-Quantsは使いません）。途中で止まっても、再実行すれば済んだ会社を飛ばします。

```bash
HEDGE_FUND_HOME=.hedge-fund .venv/bin/python japan_knowledge_probe.py \
  outputs/japan-universe-2026-02-16.json outputs/japan-universe-knowledge.json
```

3. 会社名あり・会社名なしのバックテストを順に実行します。それぞれデータの取得に約25分かかります。AIの回答は保存されるので、同じ条件で再実行すればAIの料金はかかりません。

```bash
HEDGE_FUND_HOME=.hedge-fund .venv/bin/aihf japan-30.yaml --data-provider jquants \
  --tickers "$(cat outputs/japan-universe-2026-02-16.tickers)" --backtest \
  --start 2026-02-16 --date 2026-06-26 --out outputs/japan-30-named.json
```

```bash
HEDGE_FUND_HOME=.hedge-fund .venv/bin/aihf japan-30-anon.yaml --data-provider jquants \
  --tickers "$(cat outputs/japan-universe-2026-02-16.tickers)" --backtest \
  --start 2026-02-16 --date 2026-06-26 --out outputs/japan-30-anon.json
```

4. 同じ資料に対する判断を組にして、判断役・規模の層ごとに集計します。集計の詳細は`outputs/japan-30-named-compare.json`にも保存されます。

```bash
.venv/bin/python japan_compare.py outputs/japan-30-named.json outputs/japan-30-anon.json \
  outputs/japan-universe-2026-02-16.json outputs/japan-universe-knowledge.json
```

### 結果の画像・GIF・端末リプレイ

どちらのスクリプトも保存済みの結果だけを読み、APIは呼びません（何度実行しても費用はかかりません）。社名は規模別の記号（Core30-1、Mid400-7など）で伏せ、予備的な結果であること、投資助言ではないこと、データとAIの出典を表示します。

`japan_media.py`は、`outputs/media/`にPNG 4枚（中立の割合、判断の入れ替わり、実験の仕組み、同じ数字から違う結論になった例）と、毎週の判断のGIFを作ります。いずれも1600×900です。表示にはmacOSのヒラギノ角ゴシックを使います。

```bash
.venv/bin/python japan_media.py outputs/japan-30-named.json outputs/japan-30-anon.json \
  outputs/japan-universe-2026-02-16.json outputs/media
```

`japan_replay.py`は、会社名あり・会社名なしの2つのファンドを端末に左右に並べ、週ごとに再生します（資産の推移と1306の比較、買い・空売り、大きい持ち高、その週の注文、AIの判断が変わった銘柄）。通常の速度で約65秒です。

- 画面録画では、ターミナル（またはiTerm2）のウィンドウを横170文字×縦50行以上にします。今の大きさは`stty size`で確認できます（行数、列数の順）。
- ⌘⇧5で画面収録を開始してから、下のコマンドを実行します。最後の週のあと4秒で自動終了し、途中でやめるときはCtrl+Cです。
- `--speed 1.5`で1.5倍速、`--speed 0.7`でゆっくり再生します。`--final`を付けると、アニメーションなしで最後の週だけを表示します（スクリーンショット用）。

```bash
.venv/bin/python japan_replay.py outputs/japan-30-named.json outputs/japan-30-anon.json \
  outputs/japan-universe-2026-02-16.json
```

公開する前の注意:

- 資産の推移や注文の金額はJ-Quantsの株価から計算しています。公開してよいか、J-Quantsの利用条件を確認してください。
- 両ファンドとも1306に負けており、空売りが中心です。会社名なしのほうが負けが小さいのは、期間が短く偶然の範囲です。空売りの費用や手数料も入っていません。
- 注文の多くは、判断が変わったためではなく、株価が100株の境目をまたいだことによる機械的な調整です。

## 検証結果を読むとき

- **空売り**: 上流 2.4.0 以降、日本株用の設定ファイルはすべて買いだけ（`blend: {mode: long_only}`）で、弱気の判断は「持たない」になります。2026年9月の30銘柄の比較（タグ`study-2026-09-japan-30`）では、弱気の判断は空売りでした。シミュレーションの空売りには、制度信用で売れる貸借銘柄の制限、貸株料・逆日歩、委託保証金が反映されず、実際より有利です。
- **約定**: 上流 2.4.0 以降、見直し日の判断は次の営業日の終値で約定します。期間の最後の判断は約定されず、`pending`に残ります。
- `japan-pilot.yaml` の資金は100万円、週次リバランス、単一銘柄の上限は資金の50%、注文単位は100株です。小さな配分は100株に満たず、現金で残ります。
- `1306` はTOPIX連動ETFの価格を使う比較対象です。TOPIX指数のトータルリターンではありません。
- 売買する銘柄は終値の**未調整**データを使います。momentumを使わない設定では、株数が増える分割の権利落ち日に持株数を分割比率だけ増やします（`JQuantsPriceClient(adjust_splits=True)`と`backtest_fund`）。比較対象（`benchmark`）だけは、売買対象に含まれていなければJ-Quantsの調整後価格（`AdjC`など）を使います。売買する銘柄に`AdjFactor`が1以外の日があれば停止し、分割・併合を反映した持株数の再計算は行いません。約定する日と資産を評価する日には、保有・売買する銘柄の**その日の終値**が必要で、なければ停止します（上流の`exact_marks`）。
- 配当・ETF分配金、税、手数料、スリッページ、売買インパクト、実際の終値での約定可能性は反映されません。生存銘柄だけを後から選ぶことによるバイアスも別途検証が必要です。利益や取引可能な運用成績として公表しないでください。
- J-Quants利用時に使えるモデルは`momentum`とLLMエージェント（Buffettなど）です。PEADなどそれ以外はCLIが拒否します。
- 外部にデータや分析結果を継続配布する場合は、J-Quantsの契約条件を事前に確認してください。APIキーを持たない環境では、実データでの動作は未確認です。

## 独立プロジェクトに向けた整理

### 上流の小さな変更で済む点

- 売買単位: `FundSpec.lot_size`、`build_orders`の単位切り捨て、`SimBroker`の単位チェック。既定値1で米国株の挙動は変わらないため、上流へ提案しやすい形です。
- 当日バー必須の値付け: 以前は`DataClient`の任意属性`require_current_bar`で、売買停止日に前日終値で約定させないようにしていました。上流 2.4.0 で約定日の終値が必須になり（`exact_marks`）、不要になりました。
- データ提供元の切り替え: `run.py`の`--data-provider`。上流では提供元ごとの分岐をファクトリにまとめると追加しやすくなります。
- 事前の一括取得（`preload_prices`）: レート制限のあるAPI向けの一般的な改善で、`CachedDataClient`の範囲キャッシュとして上流に入れられます。
- AI回答の読み取り: モデルが短い回答のあとに完全な回答を続けて返すと、元の処理は最初の回答を使い、判断理由が空になっていました。判断と確信度が一致する場合だけ完全な回答を使い、食い違う場合は読み取り失敗（判断を控える）にしました。
- 買いだけの制限: 以前は`RiskLimits.long_only`を加えていましたが、上流 2.4.0 の判断役ごとの買いだけ／空売りの仕組みに置き換えて外しました。
- 市場のタイムゾーン: `HEDGE_FUND_MARKET_TZ`で、確定済みの取引日の判定をニューヨーク以外の市場に合わせられます。既定値は変わりません。
- 匿名化モード: `LLMAgent`の`anonymize`引数と`FundamentalsSnapshot.render_anonymized()`。既定では無効で、上流の動作は変わりません。
- データ保存先の変更: `HEDGE_FUND_HOME`で`~/.hedge-fund/`（設定・キャッシュ・`.env`）を移せるようにしました。既定値は変わりません。
- 既存の不具合: 上流`7d897a0`の時点で`hedge_fund/tui/test_app.py::test_picker_and_masked_key_save_or_cancel`が失敗していましたが、上流 2.4.0 で直っています。

### 日本株向けに設計し直すべき点

- 銘柄マスタ: 英字入りの新コード、ETF・REITなど銘柄ごとに異なる売買単位、上場・廃止日。`lot_size`をファンド単位から銘柄単位へ移す必要があります。
- 株式分割・併合: 比較対象は調整後価格で対応済みです。売買する銘柄は、momentumを使わない設定なら株数が増える分割に対応しました（権利落ち日に持株数を掛け算。100株単位が崩れる分割は停止）。株式併合と、momentumを使う設定での分割は停止します。momentumは調整後価格で計算するよう直せば分割に対応できます。
- 約定モデル: 東証の呼値単位、値幅制限（`UL`/`LL`）、ストップ高・安で約定しない日、寄付・引けのどちらで約定させるか、手数料と税。
- 財務・決算データ: 決算短信サマリーからTTMの指標を作る部分（`hedge_fund/data/jquants_fins.py`）は実装済みです。粗利率・負債などの詳細項目（有料のBS/PL/CFかEDINET）、組み替え後の前年数値、開示時刻を使ったより細かい時点判定は未対応です。PEADは、会社予想との差など日本向けのサプライズ定義を別に設計する必要があります。
- LLMの先読み: 証券コードで会社が分かるため、学習期間内の検証はAIの判断力の評価になりません。匿名化モードは実装済みですが、数字からの推測は防げません。確実なのは、モデルの学習期間より後の期間で前向きに検証することです（無料プランは12週間遅れのため、有料プランが必要になる見込み）。
- 比較対象: 1306の価格ではなく、TOPIXの配当込み指数、またはETFの分配金を加えた系列にする。
- 生存者バイアス: 過去時点の構成銘柄（TOPIX 500など）から対象銘柄を決める仕組み。
- 暦と時刻: 東証の取引日カレンダー、大引け時刻（2024年11月から15:30）、JSTで時点を判定すること。

資料: [J-Quants V2日足仕様](https://jpx-jquants.com/en/spec/eq-bars-daily), [財務情報サマリー仕様](https://jpx-jquants.com/en/spec/fin-summary), [上場銘柄一覧仕様](https://jpx-jquants.com/en/spec/eq-master), [J-Quants](https://jpx-jquants.com/en), [JPX売買単位](https://www.jpx.co.jp/equities/trading/domestic/03.html)
